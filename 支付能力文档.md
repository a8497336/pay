# 缴费系统支付能力文档

## 目录
- [1. 支付能力概述](#1-支付能力概述)
- [2. 支付流程](#2-支付流程)
- [3. API接口文档](#3-api接口文档)
- [4. 订单状态说明](#4-订单状态说明)
- [5. 配置说明](#5-配置说明)
- [6. 前端集成](#6-前端集成)
- [7. 后台管理](#7-后台管理)
- [8. 常见问题](#8-常见问题)

---

## 1. 支付能力概述

### 1.1 支持的支付方式
- 微信支付（Native扫码支付）
- 支付宝（预留接口）
- 银行卡（预留接口）

### 1.2 核心功能
- 支付订单创建
- 支付状态查询
- 支付回调处理
- 订单状态管理
- 支付成功监听
- 订单完成确认

### 1.3 技术栈
- 后端：Node.js + Express
- 前端：Vue 3 + Vite
- 支付SDK：微信支付V3 API
- 数据存储：JSON文件存储

---

## 2. 支付流程

### 2.1 完整支付流程

```
用户提交充值订单
    ↓
前端调用创建支付订单API
    ↓
后端创建订单记录（状态：pending）
    ↓
后端调用微信支付API创建支付订单
    ↓
返回二维码给前端
    ↓
前端显示二维码弹窗
    ↓
用户扫码支付
    ↓
前端轮询查询支付状态（每3秒）
    ↓
后端调用微信支付API查询最新状态
    ↓
支付成功后更新订单状态（pending → completed）
    ↓
前端检测到支付成功，关闭弹窗，显示成功页面
    ↓
后台管理员确认充值完成（completed → success）
```

### 2.2 支付状态监听机制

#### 前端轮询（QRCodeModal.vue）
- 轮询间隔：3秒
- 轮询条件：订单状态为 `pending` 或 `completed`
- 停止条件：检测到支付成功（`tradeStatus === 'SUCCESS'`）
- 超时处理：建议设置最大轮询时间（如2分钟）

#### 后端主动查询（payment.js）
- 每次查询支付状态时，主动调用微信支付API
- 如果微信支付返回支付成功，自动更新数据库
- 支持支付回调通知（异步）

---

## 3. API接口文档

### 3.1 创建支付订单

**接口地址：** `POST /api/payment/`

**请求参数：**
```json
{
  "orderNumber": "TEST20250102001",
  "payAmount": 0.01,
  "faceAmount": 10,
  "paymentMethod": "wechat",
  "orderData": {
    "type": "fast",
    "typeName": "电费快充",
    "province": "北京",
    "city": "北京市",
    "accountNumber": "1234567890"
  }
}
```

**响应示例：**
```json
{
  "success": true,
  "orderNumber": "D1767356210380G1WU",
  "tradeNo": "LT1767356210380NY349MSD",
  "message": "支付订单创建成功",
  "qrCode": "weixin://wxpay/bizpayurl?pr=kHc0ouhz3"
}
```

**说明：**
- `orderNumber`：订单号（可选，不传则自动生成）
- `payAmount`：实付金额（单位：元）
- `faceAmount`：充值面额（单位：元）
- `paymentMethod`：支付方式（`wechat`/`alipay`/`card`）
- `qrCode`：微信支付二维码链接

---

### 3.2 查询支付状态

**接口地址：** `POST /api/payment/query`

**请求参数：**
```json
{
  "orderNumber": "D1767356210380G1WU"
}
```

**响应示例：**
```json
{
  "success": true,
  "tradeStatus": "SUCCESS",
  "totalAmount": 0.01,
  "tradeNo": "LT1767356210380NY349MSD",
  "payTime": "2026-01-02T12:14:34.366Z"
}
```

**状态说明：**
- `SUCCESS`：支付成功
- `NOTPAY`：未支付
- `USERPAYING`：用户支付中
- `PAYERROR`：支付失败
- `CLOSED`：订单已关闭

---

### 3.3 微信支付回调

**接口地址：** `POST /api/payment/wechat/notify`

**说明：**
- 微信支付服务器主动调用此接口通知支付结果
- 需要验证签名确保请求来自微信
- 返回格式：`{ code: '0000', msg: 'success' }`

---

### 3.4 更新订单状态

**接口地址：** `PUT /api/orders/:orderNumber/status`

**请求参数：**
```json
{
  "status": "success"
}
```

**响应示例：**
```json
{
  "success": true,
  "order": {
    "id": 66,
    "orderNumber": "D1767356210380G1WU",
    "status": "success",
    ...
  }
}
```

**状态说明：**
- `pending`：待支付
- `completed`：已付款
- `processing`：充值中
- `success`：已完成
- `failed`：充值失败

---

### 3.5 获取订单列表

**接口地址：** `GET /api/orders`

**查询参数：**
- `status`：订单状态（可选）
- `keyword`：订单号或户号（可选）

**示例：**
```
GET /api/orders?status=completed&keyword=123456
```

**响应示例：**
```json
[
  {
    "id": 66,
    "orderNumber": "D1767356210380G1WU",
    "type": "fast",
    "typeName": "电费快充",
    "province": "北京",
    "city": "北京市",
    "accountNumber": "1234567890",
    "accountName": "",
    "faceAmount": 100,
    "payAmount": 100,
    "status": "completed",
    "progress": 0,
    "remainingTime": 3600,
    "createTime": "2026-01-02T12:14:34.366Z",
    "paymentMethod": "wechat",
    "tradeNo": "LT1767356210380NY349MSD",
    "finishTime": null,
    "failReason": null
  }
]
```

---

## 4. 订单状态说明

### 4.1 状态流转图

```
pending（待支付）
    ↓ [用户支付]
completed（已付款）
    ↓ [管理员确认]
processing（充值中）
    ↓ [充值完成]
success（已完成）

    ↓ [支付失败/充值失败]
failed（充值失败）
```

### 4.2 状态详细说明

| 状态值 | 显示文案 | 颜色 | 说明 |
|--------|---------|------|------|
| `pending` | 待支付 | 黄色 | 订单已创建，等待用户支付 |
| `completed` | 已付款 | 蓝色 | 用户已完成支付，等待充值 |
| `processing` | 充值中 | 青色 | 充值进行中，预计到账时间 |
| `success` | 已完成 | 绿色 | 充值已完成，到账成功 |
| `failed` | 充值失败 | 红色 | 充值失败，需要退款或重新充值 |

### 4.3 状态变更规则

1. **pending → completed**
   - 触发条件：微信支付API返回支付成功
   - 自动执行：后端轮询或支付回调

2. **completed → processing**
   - 触发条件：管理员开始充值
   - 手动执行：后台管理系统

3. **processing → success**
   - 触发条件：充值完成
   - 手动执行：后台管理系统点击"完成充值"

4. **任意状态 → failed**
   - 触发条件：支付失败或充值失败
   - 手动执行：后台管理系统

---

## 5. 配置说明

### 5.1 环境变量配置（.env）

```env
# 微信支付配置
WECHAT_PAY_MCHID=1722745417
WECHAT_PAY_SERIAL_NO=3B0CB09F2C8CA61420C7BBBBBF449B115D00F352
WECHAT_PAY_PRIVATE_KEY_PATH=./certificates/apiclient_key.pem
WECHAT_PAY_CERT_PATH=./certificates/apiclient_cert.pem
WECHAT_PAY_APIV3_KEY=f2h5uhhsgs9
WECHAT_PAY_APPID=wx5d360e8e1f8459cb
WECHAT_PAY_NOTIFY_URL=http://localhost:5000/api/payment/wechat/notify

# 服务器配置
PORT=5000
```

### 5.2 微信支付证书

证书文件位置：`backend/certificates/`
- `apiclient_key.pem`：商户私钥
- `apiclient_cert.pem`：商户证书
- `apiclient_cert.p12`：PKCS12格式证书（备用）

### 5.3 数据库配置

数据库文件位置：`backend/database/`
- `orders.json`：订单数据
- `aftersales.json`：售后数据

---

## 6. 前端集成

### 6.1 支付流程集成

#### 步骤1：创建支付订单
```javascript
const orderData = {
  orderNumber: 'TEST20250102001',
  payAmount: 0.01,
  faceAmount: 10,
  paymentMethod: 'wechat',
  orderData: {
    type: 'fast',
    typeName: '电费快充',
    province: '北京',
    city: '北京市',
    accountNumber: '1234567890'
  }
}

const result = await paymentApi.createPayment(orderData)
```

#### 步骤2：显示二维码
```javascript
const qrCode = result.qrCode
const orderNumber = result.orderNumber

// 显示二维码弹窗
showQRCodeModal(qrCode, orderNumber)
```

#### 步骤3：轮询支付状态
```javascript
const startPolling = () => {
  const timer = setInterval(async () => {
    const result = await paymentApi.queryPaymentStatus(orderNumber)
    
    if (result.tradeStatus === 'SUCCESS') {
      clearInterval(timer)
      // 支付成功，关闭弹窗，显示成功页面
      showSuccessPage()
    }
  }, 3000)
}
```

### 6.2 组件说明

#### QRCodeModal.vue
- 显示微信支付二维码
- 自动轮询支付状态
- 显示当前支付状态
- 支付成功后自动关闭

#### PaymentModal.vue
- 选择支付方式
- 确认支付信息
- 调用支付API

#### SuccessModal.vue
- 显示支付成功信息
- 显示订单详情
- 提供返回首页按钮

---

## 7. 后台管理

### 7.1 订单管理页面

访问地址：`http://localhost:5000/admin.html`

#### 功能列表：
1. 订单列表展示
2. 订单状态筛选
3. 订单号/户号搜索
4. 订单统计
5. 订单状态更新

#### 操作说明：
1. **查看订单**：点击"查看详情"查看订单完整信息
2. **完成充值**：对于"已付款"状态的订单，点击"完成充值"按钮
3. **筛选订单**：使用状态筛选器快速查找订单
4. **搜索订单**：输入订单号或户号进行搜索

### 7.2 统计数据

- 总订单数：所有订单数量
- 待支付：`pending` 状态订单数
- 已付款：`completed` 状态订单数
- 充值中：`processing` 状态订单数
- 已完成：`success` 状态订单数

---

## 8. 常见问题

### 8.1 支付相关

#### Q1：支付成功后订单状态没有更新？
**A：** 检查以下几点：
1. 后端是否正常运行
2. 微信支付API是否正常返回
3. 前端轮询是否正常工作
4. 查看后端日志是否有错误信息

#### Q2：二维码扫描后没有反应？
**A：** 可能原因：
1. 订单已过期（微信支付二维码有效期2小时）
2. 网络问题导致支付失败
3. 微信支付系统异常

#### Q3：如何测试支付功能？
**A：** 使用微信支付测试账号：
1. 金额设置为0.01元
2. 扫描二维码完成支付
3. 等待几秒钟查看状态更新

### 8.2 订单相关

#### Q4：订单状态"已付款"和"已完成"有什么区别？
**A：**
- **已付款**：用户已完成支付，等待充值
- **已完成**：充值已完成，到账成功

#### Q5：如何处理支付失败的订单？
**A：**
1. 查看失败原因
2. 联系用户确认
3. 重新创建订单或退款

#### Q6：订单数据存储在哪里？
**A：** 订单数据存储在 `backend/database/orders.json` 文件中

### 8.3 技术相关

#### Q7：如何修改轮询间隔？
**A：** 修改 `QRCodeModal.vue` 中的轮询间隔：
```javascript
setInterval(async () => {
  // ...
}, 3000) // 修改这里的数字，单位：毫秒
```

#### Q8：如何添加新的支付方式？
**A：**
1. 在 `wechatPayService.js` 中添加新的支付服务
2. 在 `payment.js` 中添加对应的路由处理
3. 在前端添加支付方式选择

#### Q9：数据库如何迁移到MySQL？
**A：**
1. 安装MySQL驱动：`npm install mysql2`
2. 修改 `database.js` 中的数据库连接
3. 更新SQL语句以适配MySQL语法

---

## 9. 附录

### 9.1 文件结构

```
backend/
├── src/
│   ├── config/
│   │   └── liantuofu.js          # 联通支付配置
│   ├── models/
│   │   └── database.js            # 数据库模型
│   ├── routes/
│   │   ├── orders.js              # 订单路由
│   │   ├── payment.js             # 支付路由
│   │   └── aftersales.js         # 售后路由
│   ├── services/
│   │   ├── liantuofuService.js   # 联通支付服务
│   │   └── wechatPayService.js   # 微信支付服务
│   ├── utils/
│   │   └── progressUpdater.js     # 进度更新工具
│   └── server.js                # 服务器入口
├── certificates/                 # 支付证书
├── database/                    # 数据库文件
└── public/
    └── admin.html                # 后台管理页面

frontend/
├── src/
│   ├── api/
│   └── index.js                # API接口
│   ├── components/
│   ├── OrderList.vue            # 订单列表
│   ├── OrderDetailModal.vue     # 订单详情
│   ├── PaymentModal.vue        # 支付弹窗
│   ├── QRCodeModal.vue         # 二维码弹窗
│   ├── RechargeForm.vue        # 充值表单
│   └── SuccessModal.vue       # 成功弹窗
│   ├── stores/
│   │   └── order.js              # 订单状态管理
│   ├── views/
│   │   ├── Orders.vue            # 订单页面
│   │   ├── OrderDetail.vue       # 订单详情页
│   │   └── Recharge.vue        # 充值页面
│   └── utils/
│       └── request.js            # 请求工具
```

### 9.2 相关链接

- 微信支付官方文档：https://pay.weixin.qq.com/wiki/doc/apiv3/index.shtml
- 微信支付API文档：https://pay.weixin.qq.com/wiki/doc/apiv3/apis/index.shtml
- Node.js微信支付SDK：https://github.com/wechatpay-apiv3/wechatpay-nodejs-sdk

### 9.3 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0.0 | 2026-01-02 | 初始版本，支持微信支付 |

---

## 10. 联系方式

如有问题，请联系开发团队。
